import getJson from 'weather-app/utils/get-json';
import { module, test } from 'qunit';

module('Unit | Utility | get json');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getJson();
  assert.ok(result);
});
